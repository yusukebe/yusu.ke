{
  "scripts": {
    "dev": "wrangler dev --local --live-reload src/index.tsx",
    "deploy": "wrangler publish --minify src/index.tsx"
  },
  "dependencies": {
    "hono": "^3.1.5",
    "marked": "^4.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20221111.1",
    "@types/marked": "^4.0.8",
    "wrangler": "^2.13.0"
  }
}
